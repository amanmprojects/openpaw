Try to keep one file limited to one core feature, and include docstrings in all files and functions to explain what they do.
Commits happen per file change
Commit messages stay simple, like 'Implemented Session History', 'Improved rendering of Assitant messages in CLI', 'Fixed a critical bug in streaming of Outputs'
Version numbers (tags) are saved as 'vA.B.C' e.g. v0.3.2